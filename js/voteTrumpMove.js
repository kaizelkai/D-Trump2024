// Si vous souhaitez manipuler des propriétés via JavaScript, vous pouvez le faire ainsi :
const line = document.getElementById('line');
const time='10s';
// Optionnel : ajuster la durée de l'animation (par exemple, 10 secondes)
line.style.animationDuration = time; // Vous pouvez le changer dynamiquement si nécessaire
